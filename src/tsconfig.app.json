{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "./",
    "baseUrl": "./",
    "outDir": "../out-tsc/app",
    "types": [] // don't auto-discover @types/node, it results in a ///<reference in the .d.ts output
  },
  "exclude": [
    "../node_modules",
    "../dist",
    "**/*.spec.ts"
  ]
}
